import XCTest
@testable import ImageViewCaching

final class ImageViewCachingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ImageViewCaching().text, "Hello, World!")
    }
}
